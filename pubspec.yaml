name: exchange
description: An ultra-fast, real-time crypto exchange app demo using raw WebSockets.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.1  # Raw WebSockets for maximum performance
  intl: ^0.19.0               # Currency Formatting

dev_dependencies:
  flutter_test:
    sdk: flutter
